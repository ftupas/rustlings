// variables2.rs
//
// Execute `rustlings hint variables2` or use the `hint` watch subcommand for a
// hint.


fn main() {
    let x: u32 = 10; // x needs a type annotation and initialized value
    if x == 10 {
        println!("x is ten!");
    } else {
        println!("x is not ten!");
    }
}
